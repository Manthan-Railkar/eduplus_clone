# Server Settings
PORT=5000
NODE_ENV=development

# Database Configuration
# By default, connects to a local MongoDB instance. Update if using MongoDB Atlas or another host.
MONGODB_URI=mongodb://localhost:27017/eduplus_clone

# Authentication Settings
# Use a secure secret key in production
JWT_SECRET=eduplus_super_secret_jwt_key_2026_dev
JWT_EXPIRES_IN=7d

# CORS Configuration (React Frontend Address)
FRONTEND_URL=http://localhost:5173
